# Screen Search クローンアプリ：機能要件（Fluent Search 互換を参考にした定義）

> 対象OS: Windows（前提）  
> 目的: Fluent Search の「Screen Search」相当の “画面上の操作可能要素をキーボードだけで選択・実行できる” 機能を、常駐＋グローバルホットキー起動で提供する。  
> 重要ポイント: **バックグラウンド常駐し、ホットキーのみを捕捉**し、起動した瞬間に**アクティブウィンドウ情報（プロセス/ハンドル/座標/モニタ等）を取得**して、そのコンテキストに対してスクリーン解析とオーバーレイ表示を行う。

---

## 1. 用語

- **常駐（Resident）**: アプリ本体UIを表示せずにバックグラウンドで動作し続ける状態（トレイ常駐を含む）。
- **グローバルホットキー**: フォーカスしているアプリに関係なくOS全体で捕捉するキーコンビネーション。
- **アクティブウィンドウ**: 起動時点でフォーカスされている最前面ウィンドウ（HWND）。
- **スクリーン解析**: 画面上のクリック可能要素/テキストを推定し、ラベルを割り当てる処理。
- **ヒントラベル（Hint labels）**: 画面上の要素に被せて表示される短い文字列。ユーザーがタイプして対象を特定する（Vimium的）。:contentReference[oaicite:0]{index=0}

---

## 2. ユースケース

### UC-1: 常駐中にホットキーで Screen Search を起動
1) アプリはバックグラウンドで稼働（UIは表示されていない）  
2) ユーザーがホットキーを押す  
3) 起動トリガ時点で **アクティブウィンドウ情報を取得**  
4) 対象（アクティブウィンドウ or アクティブモニタ）をスクリーン解析  
5) オーバーレイ表示（ラベル提示）  
6) ユーザーがラベル入力→実行（クリック等）:contentReference[oaicite:1]{index=1}

### UC-2: キーボードだけで画面上の要素をクリック/操作
- ラベル入力で要素を一意に特定し、クリック/フォーカス/右クリック等を実行（詳細は後述）:contentReference[oaicite:2]{index=2}

### UC-3: 複数モニタ環境でターゲットを切り替える
- ショートカットで解析対象モニタを左右に移動し、各モニタで再解析してラベルを再描画:contentReference[oaicite:3]{index=3}

### UC-4: OCRで「画面上の文字」を検索して対象へジャンプ
- 画面に表示されているテキスト（選択できない文字含む）をOCR抽出し、クエリでマッチさせる:contentReference[oaicite:4]{index=4}

### UC-5: ゲーム/フルスクリーン/リモートデスクトップではホットキーを抑制
- 「抑制時はホットキーを“ブロック”ではなく“登録解除”する」挙動を持たせ、入力遅延を避ける:contentReference[oaicite:5]{index=5}

---

## 3. スコープ

### 3.1 In Scope（必須）
- 常駐＋グローバルホットキー捕捉
- 起動瞬間のアクティブウィンドウ情報取得（後述の取得項目を満たす）
- 解析→ラベル割当→オーバーレイ表示→キーボード操作（クリック等）
- マルチモニタ切替（最低限：左右移動＋再解析）:contentReference[oaicite:6]{index=6}
- OCRによる画面内テキスト検索（最低限：抽出→検索→該当候補の提示）:contentReference[oaicite:7]{index=7}
- 設定画面（ホットキー/抑制/ラベル文字セット/ラベルサイズなど）

### 3.2 Out of Scope（当面）
- Fluent Search本体の「PC全体検索」「ランチャー」「Everything連携」等（Screen Search以外）
- クラウド同期、チーム共有、アカウント機能
- 高度な録画/履歴/学習（“次の候補を学習する”など）

---

## 4. 機能要件（FR）

### FR-1: 常駐動作（バックグラウンド）
- FR-1.1 アプリは起動後、メインUIを閉じてもプロセスは常駐できる（トレイに最小化）。
- FR-1.2 OS起動時の自動起動オプションを提供（ON/OFF）。
- FR-1.3 常駐中は**ホットキー捕捉以外の入力フックを最小化**（必要時のみ動作させる設計）。

### FR-2: グローバルホットキー管理
- FR-2.1 Screen Search 起動ホットキーを設定できる（デフォルト例: Ctrl+M 相当）。:contentReference[oaicite:8]{index=8}
- FR-2.2 ホットキー競合検出（他アプリが同一ホットキーを使用している可能性を警告）。
- FR-2.3 **ホットキー抑制（Gaming mode）**
  - FR-2.3.1 フルスクリーンアプリがフォーカスの時は自動抑制（ON/OFF可能）:contentReference[oaicite:9]{index=9}
  - FR-2.3.2 特定プロセス名リストで抑制（game.exe / mstsc.exe 等）:contentReference[oaicite:10]{index=10}
  - FR-2.3.3 抑制時は**ホットキーを“登録解除”**し、入力遅延を避ける:contentReference[oaicite:11]{index=11}

### FR-3: 起動タイミングでの「アクティブウィンドウ情報取得」（最重要）
ホットキー押下でScreen Search起動が確定した瞬間に、以下を取得し「セッションコンテキスト」として保持する。

- FR-3.1 取得必須項目
  - アクティブウィンドウ HWND
  - プロセスID / プロセス名 / 実行ファイルパス（可能なら）
  - ウィンドウタイトル
  - ウィンドウ矩形（画面座標: left/top/right/bottom）
  - Dpiスケーリング情報（モニタDPI・拡大率）
  - 所属モニタID（どのディスプレイ上にあるか）
  - フォーカス状態（本当にforegroundか）
- FR-3.2 取得失敗時のフォールバック
  - foreground取得できない場合は「カーソル位置のモニタ」をターゲットにする
  - 権限不足（UAC/昇格）で一部情報が取れない場合は取得可能項目のみでセッション継続
- FR-3.3 セキュリティ/プライバシー制約
  - ウィンドウタイトル・プロセス名等のログ永続化はデフォルトOFF（デバッグ時のみON）

### FR-4: 解析ターゲットの定義
- FR-4.1 デフォルト解析ターゲットは「アクティブモニタ全面」または「アクティブウィンドウ領域」（設定で切替）。
- FR-4.2 解析対象の再計算条件
  - モニタ切替操作時（FR-8）
  - 明示的な再解析キー
  - 一定条件（ウィンドウが大きく移動・リサイズ）を検知した場合（任意）

### FR-5: スクリーン解析エンジン
Fluent Searchが言及するように、UI要素検出には複数アプローチがある（UI Automation / 画像認識 / OCR）。:contentReference[oaicite:12]{index=12}

最低要件として：
- FR-5.1 UI Automation（アクセシビリティツリー）由来の操作可能要素検出
  - ボタン、リンク、入力欄、メニュー項目など
- FR-5.2 画像ベースのクリック可能領域推定（最低限のサポート）
  - UIAで取れないアプリ（ゲーム/独自UI）向け
- FR-5.3 OCRエンジン
  - 画面上テキスト抽出（「選択できない文字」に対応）:contentReference[oaicite:13]{index=13}
  - 言語設定（日本語/英語）と辞書・精度設定（任意）

### FR-6: ヒントラベルの生成と表示
- FR-6.1 すべての候補要素に対し、短い文字列ラベルを割り当ててオーバーレイ表示:contentReference[oaicite:14]{index=14}
- FR-6.2 ラベル文字集合はカスタマイズ可能（例: ASDWJKL…）:contentReference[oaicite:15]{index=15}
- FR-6.3 ラベル長は候補数に応じて自動的に伸長（1文字→2文字…）
- FR-6.4 ラベルサイズ（フォント/スケール）を設定できる:contentReference[oaicite:16]{index=16}
- FR-6.5 オーバーレイは「常に最前面」で表示し、元アプリへ入力を極力流さない（必要キー以外は透過/無視）。

### FR-7: キーボード操作（アクション）
Fluent Searchの“Advanced usage”にあるように、キーボードだけで複数のクリック動作を扱える設計にする。:contentReference[oaicite:17]{index=17}

- FR-7.1 ラベル入力で候補を絞り込み、確定時にアクション実行
- FR-7.2 アクション種別（最低限）
  - 左クリック
  - 右クリック
  - ダブルクリック
  - フォーカス/入力欄へカーソル移動（可能なら）
- FR-7.3 キャンセル/終了
  - ESCで即終了し、オーバーレイを破棄
- FR-7.4 連続実行モード（任意）
  - クリック後もオーバーレイを維持して次の操作へ（ON/OFF）

### FR-8: マルチモニタ対応
- FR-8.1 “右/左”のショートカットで対象モニタを切替し、そのモニタを再解析して新ラベルを描画:contentReference[oaicite:18]{index=18}
- FR-8.2 どのモニタが対象か視覚的にわかるUI（枠線/薄いシェードなど）

### FR-9: OCR検索UI（テキストで画面を探す）
Fluent SearchのOCR説明にあるように「画面上テキストを検索」できること。:contentReference[oaicite:19]{index=19}

- FR-9.1 起動後、クエリ入力モードに入れる（例: “Screen: xxx” 的なスコープでも良い）:contentReference[oaicite:20]{index=20}
- FR-9.2 OCR抽出結果から、クエリ一致の候補をハイライト/一覧化
- FR-9.3 候補選択でその位置へジャンプ（ラベル付け or フォーカス移動）

### FR-10: 設定・カスタマイズ
- FR-10.1 ホットキー設定（変更・無効化）
- FR-10.2 抑制設定（フルスクリーン時 / プロセス除外）:contentReference[oaicite:21]{index=21}
- FR-10.3 ラベル文字セット、ラベルサイズ等の外観設定:contentReference[oaicite:22]{index=22}
- FR-10.4 解析ターゲット（ウィンドウのみ/モニタ全体）切替
- FR-10.5 解析エンジン優先順位（UIA優先、画像優先、OCR併用など）

### FR-11: 可観測性（デバッグ用）
- FR-11.1 “いまアクティブウィンドウとして何を掴んだか” を確認できる診断画面
- FR-11.2 解析にかかった時間、候補数、エンジン別ヒット数を表示（開発者向け）

---

## 5. 非機能要件（NFR）

### NFR-1: 低遅延
- ホットキー押下→オーバーレイ表示まで体感 150ms〜300ms 目標（理想）。  
  ※現実にはOCRが重いので「先にUIA/画像で出してOCRは後追い」など段階表示を許容。

### NFR-2: 低負荷（常駐前提）
- 常駐中はCPUをほぼ消費しない（イベント駆動）。
- メモリ使用量の上限目標を定める（例: 常駐50〜150MB以内など）。

### NFR-3: 安定性
- 例外で常駐が落ちても自動再起動（オプション）。
- オーバーレイ描画失敗時は安全に終了し入力を阻害しない。

### NFR-4: セキュリティ/プライバシー
- 画面キャプチャ/OCR結果を**デフォルトで永続化しない**。
- ネットワーク送信はデフォルト禁止（オフライン動作）。
- 権限昇格が必要なケースは明示し、最小権限で動作。

### NFR-5: 互換性
- 主要Windowsバージョン（例: 10/11）で動作
- 高DPI/複数モニタ/異なるスケーリング混在で座標ズレを起こさない

---

## 6. 画面・状態遷移（簡易）

- Idle（常駐）  
  ↓ Hotkey  
- CaptureContext（アクティブウィンドウ情報取得）  
  ↓  
- Analyze（UIA/画像/OCR）  
  ↓  
- OverlayActive（ラベル表示・入力受付）  
  ↓ label確定 / ESC  
- ExecuteAction（クリック等）  
  ↓（連続モードならOverlayActiveへ戻る）  
- End（破棄してIdleへ）

---

## 7. 受け入れ基準（Examples）

- AC-1: 常駐中、任意アプリが前面でもホットキーでScreen Searchが起動する。
- AC-2: 起動の瞬間に、アクティブウィンドウの HWND/プロセス名/タイトル/矩形/モニタ が取得でき、診断画面で確認できる。
- AC-3: ラベル入力だけで画面上のボタンをクリックできる（マウス不要）。
- AC-4: 2枚以上のモニタで、左右切替キーで対象モニタが変わり、各モニタでラベルが再生成される:contentReference[oaicite:23]{index=23}
- AC-5: フルスクリーン時にホットキー抑制ONなら、誤起動せず入力遅延も発生しない（登録解除方式）:contentReference[oaicite:24]{index=24}
- AC-6: OCR検索で、選択できない文字が表示されている画面でも一致候補が出る:contentReference[oaicite:25]{index=25}

---

## 8. あなたの要件で落としがちな盲点（先に潰す）

1) **「ホットキーを捕捉するだけ」でも、実装によっては入力遅延の原因になる**  
   → 抑制時は“ブロック”ではなく“登録解除”に寄せる（Fluent Searchの思想）:contentReference[oaicite:26]{index=26}

2) **アクティブウィンドウ取得はDPI/仮想デスクトップ/昇格権限で破綻しやすい**  
   → 取得失敗時のフォールバック（モニタ基準）を要件に含める（FR-3.2）。

3) **OCRは重い**  
   → “常にOCR”にすると体感が終わる。段階表示・遅延実行を最初から要件化しておく。

---

## 9. 参考（Screen Searchの挙動イメージ）
- Screen Searchは「画面上の操作可能要素へラベルを被せ、キーボードだけで操作する」コンセプト:contentReference[oaicite:27]{index=27}
- OCRで画面上のテキスト検索を補完:contentReference[oaicite:28]{index=28}
- マルチモニタ切替で再解析する導線:contentReference[oaicite:29]{index=29}
- ホットキー抑制は登録解除で遅延を避ける:contentReference[oaicite:30]{index=30}