# 検証内容
- 対象OS:MacOS
- Macのネイティブツールバーにアイコンを配置し、クリックするとポップアップのような形でアイコン下部に任意のUIを表示させられる。
- 基本的にアプリはバックグラウンドで実行されていてGUIを持たず、ツールバーにアイコンが配置されるのみである。
- ツールバーをクリックするとポップアップUIが表示され、ポップアップUI内のボタンを押すとGUIが表示される。

# 技術要件
- dotnet10
- AvaloniaUI

